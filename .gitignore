# Local-only files
.DS_Store
*.swp
.deploy-token

# Keep snapshots out of git by default to keep the repo lean.
# Comment this out if you want snapshots versioned.
snapshots/*.json
!snapshots/.gitkeep
